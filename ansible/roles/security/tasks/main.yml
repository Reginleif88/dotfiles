---
# Security — sshd, firewall

# # ── SSH daemon ───────────────────────────────────────────────────
# - name: Enable and start sshd
#   ansible.builtin.systemd:
#     name: sshd
#     state: started
#     enabled: yes
#   become: yes

# # ── Firewall (ufw) ──────────────────────────────────────────────
# - name: Allow SSH through firewall
#   community.general.ufw:
#     rule: allow
#     port: "22"
#     proto: tcp
#   become: yes

- name: Enable ufw
  community.general.ufw:
    state: enabled
  become: yes
